# Fix Issues #4, #5, #6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3개 버그(스택 트레이스 노출, 스킬 파라미터 undefined, fetch 에러 메시지 모호)를 수정한다.

**Architecture:** 각 버그를 독립적으로 수정한다. #4는 provider 연결 전 도메인 사전 검증, #5는 use_skill 파라미터 디버깅 및 수정, #6은 fetch 에러에 원인(cause) 포함.

**Tech Stack:** TypeScript, Zod, Vitest

---

## File Structure

```
변경:
├── src/cli/app.ts                    # #4: provider 도메인 사전 검증
├── src/security/restricted-fetch.ts  # #4: 에러 메시지 개선
├── src/tools/use-skill.ts            # #5: 파라미터 이름 수정
├── src/tools/web-fetch.ts            # #6: fetch 에러 cause 포함
├── src/tools/api-call.ts             # #6: 동일 패턴 적용
├── tests/security/restricted-fetch.test.ts  # #4: 새 테스트
├── tests/tools/use-skill.test.ts     # #5: 파라미터 테스트 보강
├── tests/tools/web-fetch.test.ts     # #6: 에러 메시지 테스트
```

---

### Task 1: Fix #4 — Provider 도메인 사전 검증 + 스택 트레이스 제거

**문제:** LLM provider URL이 allowedDomains에 없으면 `restricted-fetch.ts:6:19` 스택 트레이스가 출력됨.

**수정:**
1. `app.ts`에서 chat 시작 전 provider baseURL의 도메인이 allowedDomains에 있는지 검증
2. 없으면 친절한 에러 메시지 + config 수정 안내 출력 후 종료

**Files:**
- Modify: `src/cli/app.ts`
- Modify: `src/security/restricted-fetch.ts`
- Test: `tests/security/restricted-fetch.test.ts`

- [ ] **Step 1: Write test for provider domain validation**

`tests/security/restricted-fetch.test.ts`에 추가:

```typescript
  it('error message includes config hint', async () => {
    await expect(
      restrictedFetch('https://evil.com/data')
    ).rejects.toThrow('Domain not allowed: evil.com');
  });
```

- [ ] **Step 2: Add provider domain pre-check in app.ts**

`src/cli/app.ts` — chat action에서 `initProvider` 호출 전에 검증:

```typescript
import { isDomainAllowed } from '../security/domain-guard.js';

// In chat action, after loadConfig:
const providerHostname = new URL(config.provider.baseURL).hostname;
if (config.security.allowedDomains.length > 0 && !isDomainAllowed(providerHostname, config.security.allowedDomains)) {
  renderError(
    `LLM provider domain "${providerHostname}" is not in allowedDomains.\n` +
    `  Add "${providerHostname}" to security.allowedDomains in your config file.`
  );
  process.exit(1);
}
```

- [ ] **Step 3: Run tests**

```bash
pnpm test
```

- [ ] **Step 4: Commit**

```bash
git commit -m "fix(#4): validate provider domain and remove stack trace"
```

---

### Task 2: Fix #5 — use_skill 파라미터 이름 문제

**문제:** LLM이 `use_skill`을 호출할 때 `skillName` 대신 다른 키(예: `name`)로 파라미터를 전달하여 undefined가 됨.

**수정:**
1. 파라미터 이름을 `skillName`에서 `name`으로 변경 (LLM이 더 자연스럽게 사용)
2. Zod describe를 더 명확하게 수정
3. 등록되지 않은 스킬 호출 시 에러 메시지에 사용 가능한 스킬 목록 표시 (이미 있음, 확인)

**Files:**
- Modify: `src/tools/use-skill.ts`
- Modify: `tests/tools/use-skill.test.ts`

- [ ] **Step 1: Change parameter name from skillName to name**

`src/tools/use-skill.ts`:

```typescript
const parameters = z.object({
  name: z.string().describe('The exact name of the skill to load (e.g. "failure-analysis")'),
});
```

execute 함수도 `{ name }` 으로 변경.

- [ ] **Step 2: Update tests**

`tests/tools/use-skill.test.ts`에서 모든 `{ skillName: ... }`를 `{ name: ... }`로 변경.

- [ ] **Step 3: Run tests**

```bash
pnpm test
```

- [ ] **Step 4: Commit**

```bash
git commit -m "fix(#5): rename use_skill parameter from skillName to name"
```

---

### Task 3: Fix #6 — fetch 에러 메시지에 원인(cause) 포함

**문제:** web_fetch가 "fetch failed"만 반환하여 디버깅 불가. TLS 에러, DNS 실패, redirect 문제 등 원인을 알 수 없음.

**수정:**
1. `web-fetch.ts`와 `api-call.ts`의 catch 블록에서 `error.cause`도 포함
2. 에러 메시지 형식: `TOOL FAILED: fetch failed (cause: getaddrinfo ENOTFOUND ...)`

**Files:**
- Modify: `src/tools/web-fetch.ts`
- Modify: `src/tools/api-call.ts`
- Test: `tests/tools/web-fetch.test.ts`

- [ ] **Step 1: Write test for detailed error message**

`tests/tools/web-fetch.test.ts`에 추가:

```typescript
  it('includes error cause in message', async () => {
    const cause = new Error('getaddrinfo ENOTFOUND example.com');
    const fetchError = new Error('fetch failed');
    fetchError.cause = cause;
    const mockFetch = vi.fn().mockRejectedValue(fetchError);

    const webFetch = createWebFetchTool(mockFetch as unknown as typeof fetch);
    const result = await webFetch.execute({ url: 'https://example.com' });

    expect(result.error).toContain('fetch failed');
    expect(result.error).toContain('getaddrinfo ENOTFOUND');
  });
```

- [ ] **Step 2: Update web-fetch.ts error handling**

`src/tools/web-fetch.ts` — doFetch catch 블록:

```typescript
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    const cause = err instanceof Error && err.cause instanceof Error ? ` (cause: ${err.cause.message})` : '';
    return {
      error: `TOOL FAILED: ${message}${cause}. You MUST report this error to the user. Do NOT make up or guess the content.`,
    };
  }
```

- [ ] **Step 3: Apply same pattern to api-call.ts**

`src/tools/api-call.ts` — doApiCall catch 블록에 동일 패턴 적용.

- [ ] **Step 4: Run tests**

```bash
pnpm test
```

- [ ] **Step 5: Commit**

```bash
git commit -m "fix(#6): include error cause in fetch failure messages"
```

---

### Task 4: Close test issue + verify

- [ ] **Step 1: Close issue #2 (test issue)**

```bash
gh issue close 2 --repo fanpa/PrivateClaw --reason completed
```

- [ ] **Step 2: Run all tests and build**

```bash
pnpm test && pnpm build
```

- [ ] **Step 3: Push and create PR**

```bash
git push -u origin fix/issue-4-5-6
gh pr create --title "fix: issues #4, #5, #6" ...
```
